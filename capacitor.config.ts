import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Edublind.app',
  appName: 'EduBlind',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
